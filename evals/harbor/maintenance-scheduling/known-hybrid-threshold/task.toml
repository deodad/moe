schema_version = "1.4"

[task]
name = "moe/maintenance-scheduling-known-hybrid-threshold"
version = "0.1.0"
description = "Store a supported time deadline while preserving the mileage alternative."
keywords = ["moe", "maintenance", "scheduling", "date", "vehicle"]

[metadata]
category = "product-agent"
difficulty = "medium"

[agent]
timeout_sec = 180.0

[verifier]
timeout_sec = 30.0

[environment]
network_mode = "no-network"
cpus = 1
memory_mb = 512
